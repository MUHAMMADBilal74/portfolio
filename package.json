{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "My Portfolio Site",
  "homepage": "https://username.github.io/repository-name",
  "main": "index.js",
  "scripts": {
    "build": "next build",
    "export": "next export",
    "deploy": "npm run build && npm run export && git add . && git commit -m 'deploy' && git push"
  },
  "dependencies": {
    "next": "^12.0.0",
    "react": "^17.0.0",
    "react-dom": "^17.0.0"
  },
  "devDependencies": {
    "eslint": "^7.32.0",
    "eslint-config-next": "^12.0.0"
  }
}

